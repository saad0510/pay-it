name: pay_it
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  lottie: ^3.2.0
  flutter_riverpod: ^2.6.1
  flutter_animate: ^4.5.2
  local_auth: ^2.3.0
  emv_card_reader:
    path: packages/emv_card_reader/
  firebase_core: ^3.9.0
  firebase_messaging: ^15.1.6
  cloud_firestore: ^5.6.0
  flutter_local_notifications: ^18.0.1
  firebase_auth: ^5.3.4
  device_friendly_name: ^1.1.0
  sensors_plus: ^6.1.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/animations/nfc_reading.json
    - assets/animations/fingerprint.json
    - assets/images/mastercard.png
    - shorebird.yaml

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
